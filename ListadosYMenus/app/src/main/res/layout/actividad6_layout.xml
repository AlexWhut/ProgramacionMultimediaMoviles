<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="10dp">

    <!-- Parte superior: Lista + Panel de detalles -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:orientation="horizontal">

        <!-- Lista de versiones -->
        <ListView
            android:id="@+id/listViewVersiones"
            android:layout_width="150dp"
            android:layout_height="match_parent"
            android:background="#F0F0F0"
            android:divider="@android:color/darker_gray"
            android:dividerHeight="1dp"/>

        <!-- Panel de detalles (derecha) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/textViewTituloDetalle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Título"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@android:color/holo_blue_dark"
                android:layout_marginBottom="10dp"/>

            <TextView
                android:id="@+id/textViewDescripcionDetalle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Descripción corta de la versión..."
                android:textSize="16sp"
                android:textColor="@android:color/black"/>

        </LinearLayout>
    </LinearLayout>

    <!-- Parte inferior: Descripción larga -->
    <TextView
        android:id="@+id/textViewDescripcionLarga"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Descripción completa de la versión seleccionada..."
        android:textSize="18sp"
        android:padding="16dp"
        android:background="#E8EAF6"
        android:layout_marginTop="10dp"/>

</LinearLayout>
